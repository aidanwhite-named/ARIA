[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
# 실제 CLI 를 호출하는 테스트는 기본 실행에서 제외한다.
# 사용자가 직접 돌릴 때만:  pytest -m live_cli
markers =
    live_cli: 실제 CLI 와 계정 사용량이 필요한 opt-in 테스트
addopts = -m "not live_cli" -q
